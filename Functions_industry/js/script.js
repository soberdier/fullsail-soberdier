// Functions industry

var pages;
var deadLine;
var overTime;
function takeJob (pages,deadLine,overTime){
	var pages = prompt("How many pages would you like the website to be?")
	var deadLine = prompt("When do you want the site completed?")
	var overTime = prompt("Are you willing to pay overtime if I have to work more than 40 hours?")
	if (overtime = no){alert("I am sorry I can not take the job.")}
	
	}